This is an existing Next.js project connected with Sanity CMS.
The entire UI, layout, styling, and structure are already built.

⚠️ STRICT RULES (DO NOT BREAK):

1. Do NOT change any UI design.
2. Do NOT modify layout, styling, Tailwind classes, or structure.
3. Do NOT refactor existing components.
4. Do NOT remove or rewrite existing logic.
5. Do NOT create unnecessary files.
6. Only add what is strictly required for multilingual functionality.
7. Keep Navbar structure exactly the same.
8. Do NOT change routing unless absolutely necessary.
9. Do NOT touch any unrelated logic.

--------------------------------------------------

🎯 OBJECTIVE:

Implement full website multilingual functionality including:

1) Sanity CMS dynamic content
2) Static frontend content (navbar, buttons, headings, etc.)

--------------------------------------------------

🌍 LANGUAGES REQUIRED:

- English (default)
- Hindi
- Arabic
- Russian

--------------------------------------------------

🧩 SANITY REQUIREMENTS:

For ALL existing schemas:

1. Add a language selection system when creating or editing content.
2. English must be default.
3. While adding content in Sanity Studio, there must be an option to choose which languages this content supports (checkbox or multi-select).
4. Example: User selects English + Arabic + Hindi.
5. Content fields must support multilingual structure.

Example structure:

content: {
  en: "...",
  hi: "...",
  ar: "...",
  ru: "..."
}

6. If a language is not selected or not filled,
   fallback must automatically be English.

7. This multilingual logic must be added to ALL schemas that contain text fields (blog, property, pages, etc.).

8. After selecting languages and publishing,
   frontend must display content based on selected Navbar language.

--------------------------------------------------

💻 FRONTEND REQUIREMENTS:

1. Implement global language state (Context or similar).
2. When Navbar language changes:
   - All static content must change (via JSON translation files).
   - All Sanity content must change dynamically.
3. If selected language content does not exist:
   → Automatically fallback to English.

--------------------------------------------------

📁 STATIC CONTENT (Non-Sanity):

1. Create translation JSON files for:
   - en
   - hi
   - ar
   - ru

2. Do NOT hardcode translations inside components.
3. Use a clean translation function like:
   t("home")

4. Entire website text must update when language changes.

--------------------------------------------------

📦 IMPORTANT:

- Do NOT duplicate pages per language.
- Do NOT change URL structure.
- Do NOT rebuild the project.
- Do NOT break existing functionality.
- Keep implementation clean and production-ready.

--------------------------------------------------

FINAL RESULT:

✔ When adding content in Sanity → user chooses supported languages.
✔ English remains default.
✔ Missing language → fallback to English.
✔ Navbar language switch updates entire website.
✔ Both Sanity and non-Sanity content translate.
✔ No UI changes.
✔ No unnecessary files.
✔ No structural modifications.
